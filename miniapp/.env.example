# Only needed if the Mini App is deployed on a different origin than the
# backend (the common case - see README.md's "static bundle to a
# CDN/static host" note). Leave unset for same-origin/local dev.
# VITE_API_BASE_URL=https://your-backend.example.com
