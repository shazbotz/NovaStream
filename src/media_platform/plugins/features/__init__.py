"""Feature plugins land here in Phase 3 onward (catalog_search, streaming,
download, mini_app, genre_browsing, continue_watching, inline_search,
broadcast, statistics, notifications, admin_dashboard, channel_access -
see architecture-design-phase1-v3.md §8 for the full inventory and what's
intentionally excluded).

Deliberately empty in this bootstrap phase: the plugin kernel must prove
it can discover and load zero feature plugins without erroring before any
feature is implemented.
"""
